const year=document.querySelector('#year');if(year)year.textContent=new Date().getFullYear();
const cursor=document.querySelector('.cursor-glow');if(cursor&&matchMedia('(pointer:fine)').matches){addEventListener('pointermove',e=>{cursor.style.left=`${e.clientX}px`;cursor.style.top=`${e.clientY}px`})}
const words=['STUDENT DEVELOPER','DIGITAL BUILDER','WEB / MOBILE / BACKEND','UI / UX EXPLORER','HARDWARE & SYSTEMS'];const typed=document.querySelector('#typed');let wi=0,ci=0,del=false;function typeLoop(){if(!typed)return;const word=words[wi];typed.textContent=del?word.slice(0,ci--):word.slice(0,ci++);if(!del&&ci>word.length){del=true;return setTimeout(typeLoop,1100)}if(del&&ci<0){del=false;wi=(wi+1)%words.length;ci=0}setTimeout(typeLoop,del?30:60)}typeLoop();
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.project,.matrix-grid article,.about-command,.about-copy,.section-head').forEach(e=>observer.observe(e));
document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const t=document.querySelector(a.getAttribute('href'));if(t){e.preventDefault();t.scrollIntoView({behavior:'smooth'})}}));
document.querySelectorAll('.project').forEach(card=>{card.addEventListener('pointermove',e=>{if(!matchMedia('(pointer:fine)').matches)return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(1000px) rotateX(${y*-1.4}deg) rotateY(${x*1.4}deg) translateY(-5px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});
const style=document.createElement('style');style.textContent=`.hero-grid{display:grid!important}.terminal{display:block!important}.project-screen{display:flex!important}.project-info{display:grid!important}.matrix-grid article,.about-command,.about-copy,.section-head{transition:opacity .65s ease,transform .65s ease}.matrix-grid article:not(.visible),.about-command:not(.visible),.about-copy:not(.visible),.section-head:not(.visible){opacity:0;transform:translateY(20px)}.matrix-grid article.visible,.about-command.visible,.about-copy.visible,.section-head.visible{opacity:1;transform:none}.project{transform-style:preserve-3d}.project-screen strong{font-family:var(--mono)!important}`;document.head.appendChild(style);
