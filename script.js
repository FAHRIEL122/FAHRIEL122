const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);const year=$('#year');if(year)year.textContent=new Date().getFullYear();const glow=$('.cursor-glow');if(glow&&matchMedia('(pointer:fine)').matches){addEventListener('pointermove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'})}const typed=$('#typed'),words=['PELAJAR PENGEMBANG','PEMBANGUN DIGITAL','WEB / MOBILE / BACKEND','EKSPLORASI UI / UX','PERANGKAT KERAS & SISTEM'];let wi=0,ci=0,del=false;function type(){if(!typed)return;const w=words[wi];typed.textContent=w.slice(0,ci);if(!del){ci++;if(ci>w.length){del=true;return setTimeout(type,1100)}}else{ci--;if(ci<0){ci=0;del=false;wi=(wi+1)%words.length}}setTimeout(type,del?28:58)}type();const reveal=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');reveal.unobserve(e.target)}}),{threshold:.12});$$('.section-head,.console,.about-copy,.project,.skills article,.process,.contact .actions').forEach(e=>{e.classList.add('reveal');reveal.observe(e)});$$('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const t=$(a.getAttribute('href'));if(t){e.preventDefault();t.scrollIntoView({behavior:'smooth'})}}));if(matchMedia('(pointer:fine)').matches){$$('.project').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(1200px) rotateX(${y*-1.5}deg) rotateY(${x*1.5}deg) translateY(-6px)`});card.addEventListener('pointerleave',()=>card.style.transform='')})}const name=$('.name-lockup');if(name){name.addEventListener('pointerenter',()=>name.classList.add('charged'));name.addEventListener('pointerleave',()=>name.classList.remove('charged'))}$$('.project-art').forEach(art=>art.addEventListener('pointermove',e=>{const r=art.getBoundingClientRect();art.style.setProperty('--mx',`${(e.clientX-r.left)/r.width*100}%`);art.style.setProperty('--my',`${(e.clientY-r.top)/r.height*100}%`)}));