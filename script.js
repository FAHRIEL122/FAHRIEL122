const year=document.querySelector('#year');if(year)year.textContent=new Date().getFullYear();
const cursor=document.querySelector('.cursor');if(cursor&&matchMedia('(pointer:fine)').matches){addEventListener('pointermove',e=>{cursor.style.left=`${e.clientX}px`;cursor.style.top=`${e.clientY}px`})}
const lines=['INITIALIZING FAHRIEL.OS...','LOADING DIGITAL SYSTEMS...','WEB / MOBILE / BACKEND / SYSTEMS','SCANNING PROJECT DATABASE...','STATUS: LEARNING // BUILDING'];const typing=document.querySelector('#typing');let li=0,ci=0,erase=false;
function type(){if(!typing)return;const s=lines[li];typing.textContent=s.slice(0,ci);if(!erase&&ci<s.length){ci++;return setTimeout(type,45)}if(!erase){erase=true;return setTimeout(type,1200)}if(ci>0){ci--;return setTimeout(type,22)}erase=false;li=(li+1)%lines.length;setTimeout(type,300)}type();
const reveal=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.animate([{opacity:0,transform:'translateY(22px)'},{opacity:1,transform:'translateY(0)'}],{duration:650,easing:'cubic-bezier(.2,.8,.2,1)',fill:'forwards'});reveal.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.project,.matrix article,.console,.bio,.section-title').forEach(x=>reveal.observe(x));
const links=document.querySelectorAll('a[href^="#"]');links.forEach(a=>a.addEventListener('click',e=>{const t=document.querySelector(a.getAttribute('href'));if(t){e.preventDefault();t.scrollIntoView({behavior:'smooth'})}}));
const projects=document.querySelectorAll('.project');projects.forEach(card=>{card.addEventListener('pointermove',e=>{if(!matchMedia('(pointer:fine)').matches)return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(1000px) rotateX(${y*-1.5}deg) rotateY(${x*1.5}deg) translateY(-6px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});
setInterval(()=>{document.querySelectorAll('.visual strong').forEach(el=>{if(Math.random()>.78){el.style.textShadow='-2px 0 var(--pink),2px 0 var(--cyan)';setTimeout(()=>el.style.textShadow='',100)}})},900);
